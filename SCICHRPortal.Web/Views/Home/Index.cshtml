@using Microsoft.AspNetCore.Authorization
@using Microsoft.Extensions.Configuration;
@using SCICHRPortal.Utility.Securities;

@inject IAuthorizationService AuthorizationService
@inherits AuthorizePage<TModel>
@{
    ViewData["Title"] = "Home Page";
    Layout = "~/Views/Shared/_HomeLayout.cshtml";
}
@{
    bool isAuthorize = await IsAuthorize();
    bool isAdmin = await HasRoleAsync("Administrator");
    // bool isRegistrar = await HasRoleAsync("Admission");
    // bool isParent = await HasRoleAsync("Parent");
    // bool isStudent = await HasRoleAsync("Student");
    // bool isPrincipal = await HasRoleAsync("Principal");
    // bool isTeacher = await HasRoleAsync("Teacher Elem");
    //bool isTeacher = await HasRoleByArrayAsync(new string[] { "Teacher Elem", "Teacher Elem" });
    // bool isCoordinator = await HasRoleAsync("Coordinator");
    // bool isLibrarian = await HasRoleAsync("Librarian");
    // bool isSuppliesPersonnel = await HasRoleAsync("SuppliesPersonnel");
}
@section Styles {
    <link href="~/css/pages/home/home.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Inter&display=swap" rel="stylesheet" />
}
    <!-- Navigation Bar -->
    <nav class="navbar">
        <div class="navbar-left">
        <img src="/images/SCIC_TextLogoBelow.png" alt="Sta.Clara" class="navbar-logo" />
        </div>
        <div class="navbar-right">
        <div class="nav-action" id="logoutBtn">
            <a id="log-out">
                <span class="nav-icon logout-icon">&#10140;</span>
                <span class="nav-label">Logout</span>
            </a>
        </div>
        </div>
        
    </nav>

    <!-- Main Content -->
    <div class="portal-container">

        <!-- Background -->
        <div class="bg-blob"></div>
        <h1 class="portal-title">SCIC Digital Portal</h1>

        <!-- Module grid-->
        <div class="module-grid" id="moduleGrid">
            

           @if (isAuthorize)
        {
            <div class="module-card">
                <a asp-controller="Administration" asp-area="" asp-action="Announcement">
                    <img src="/images/administration/humanresources.png" alt="Administration" class="module-icon" />
                </a>
                <span class="module-label">Timekeeping</span>
            </div>
        }

        @*     @if (isAdmin || isRegistrar || isParent)
    {
        <div class="col-lg-4 mb-lg-3 mx-auto dashboard-icon-container">
            <a asp-controller="Dashboard" asp-area="" asp-action="Announcement">
                <img id="deur-entries-logo" class="dashboard-icon" src="/images/features/register.png">
            </a>
            <span class="dashboard-icon-title">Enrolment</span>
        </div>
    }
    @if (isAuthorize)
    {
        <div class="col-lg-4 mb-lg-3 dashboard-icon-container">
            <a asp-controller="Grading" asp-area="" asp-action="Announcement">
                <img id="deur-entries-logo" class="dashboard-icon" src="/images/features/exam.png">
            </a>
            <span class="dashboard-icon-title">Grading</span>
        </div>
    }

@if (isStudent || isPrincipal || isParent || isTeacher || isCoordinator || isAdmin || isRegistrar)
    {
        <div class="col-lg-4 mb-lg-3 mx-auto dashboard-icon-container">
            <a asp-controller="Schedule" asp-area="" asp-action="Announcement">
                <img id="deur-entries-logo" class="dashboard-icon" src="/images/features/timetable.png">
            </a>
            <span class="dashboard-icon-title">Schedule</span>
        </div>
    }
    @if (isAuthorize)
    {
        <div class="col-lg-4 mb-lg-3 mx-auto dashboard-icon-container">
            <a asp-controller="Supplies" asp-area="" asp-action="Announcement">
                <img id="deur-entries-logo" class="dashboard-icon" src="/images/features/stationery.png">
            </a>
            <span class="dashboard-icon-title">Supplies</span>
        </div>
    }
    @if (isAuthorize)
    {
        <div class="col-lg-4 mb-lg-3 mx-auto dashboard-icon-container">
            <a asp-controller="Library" asp-area="" asp-action="Announcement">
                <img id="deur-entries-logo" class="dashboard-icon" src="/images/features/books.png">
            </a>
            <span class="dashboard-icon-title">Library</span>
        </div>

    } *@


    </div>
    </div>


    @section Scripts {
        @{
         await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/js/helpers/cookie-helper.js" asp-append-version="true"></script>
    <script src="~/js/pages/home/home.js" asp-append-version="true"></script>
    }

